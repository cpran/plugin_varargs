before_script:
  - rm -rf ~/.praat-dir/plugin_testsimple
  - rm -rf ../plugin_testsimple
  - git clone https://gitlab.com/cpran/plugin_testsimple.git ../plugin_testsimple
  - cp -fa . ~/.praat-dir/plugin_varargs
  - cp -fa ../plugin_testsimple ~/.praat-dir/plugin_testsimple

# This file is generated by GitLab CI
Praat 5.4.22:
  script:
    - prove -v --exec "praat-5.4.22 --ansi --pref-dir=~/.praat-dir"
    - rm -rf ~/.praat-dir/plugin_testsimple ../plugin_testsimple
    - rm -rf ~/.praat-dir/plugin_varargs"
  tags:
    - cpran

Praat 6.0.03:
  script:
    - prove -v --exec "praat-6.0.03 --ansi --pref-dir=~/.praat-dir --run"
    - rm -rf ~/.praat-dir/plugin_testsimple ../plugin_testsimple
    - rm -rf ~/.praat-dir/plugin_varargs"
  tags:
    - cpran
